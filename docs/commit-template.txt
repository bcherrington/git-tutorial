# Commit Message template
# ----------------

<type>(scope): <short summary> (ABC-123)

# <type>(scope): <summary> (ABC-123)
# Type must be one of:
#   feat | fix | docs | style | refactor | perf | test | chore | build | ci | revert
# Add '!' after type to indicate breaking change.
# Examples:
#   feat(auth): add TOTP login (ABC-123)
#   fix(api/user)!: change 500→404 for missing user (ABC-456)


<body>

# Body (what + why, wrapped at ~72 cols)
# Explain context, motivation, design decisions.
# Example:
# Adds a 6-digit TOTP factor to augment password login.
# Supports RFC 6238-compatible apps.


<footer>
<Jira Smart Commits>
<References>
<Breaking change note>

# Footers (Smart Commits + references)
# ABC-123 #comment Implemented verify endpoint
# ABC-123 #time 1h
# ABC-123 #transition In Review
# Refs: ABC-123
# BREAKING CHANGE: users must enroll TOTP device on next login
